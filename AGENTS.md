# AGENTS.md

## Fork Notice

**This repository is a fork.** Upstream is [`pingdotgg/t3code`](https://github.com/pingdotgg/t3code) (`upstream/main`); this fork lives at [`imabdulazeez/t3code`](https://github.com/imabdulazeez/t3code) (`origin`). The fork is periodically synced with upstream and re-applies its own additions on top. The fork-specific changes relative to `upstream/main` are tracked in the [Fork Changes vs Upstream](#fork-changes-vs-upstream) table below.

## Fork Changes vs Upstream

> **IMPORTANT:** When making any change to this fork, update this table in the same change. Add a new row (or amend an existing one) describing the change, the files touched, and the key changes. Keeping this table current is mandatory.

| Change                                      | Description                                                                                                                                                                                                                          | Files touched                                                                                                                                                                                                                                                                                                                                                                                                                                | Key changes                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Rebrand to A3 + build timestamps            | Rebrand the app from upstream naming/stage labels to "A3" and surface build timestamps in the desktop artifact and UI.                                                                                                               | `apps/desktop/src/app/forkBranding.ts`, `apps/desktop/src/app/DesktopAppIdentity.ts`, `apps/web/src/forkBranding.ts`, `packages/shared/src/buildTimestamp.ts`, `scripts/build-desktop-artifact.ts`                                                                                                                                                                                                                                           | <ul><li>Stage label renamed Alpha → AA → A3</li><li>Centralized fork branding helpers for desktop + web</li><li>Build timestamps switched from UTC to local time</li><li>Build timestamp embedded in desktop artifact filenames</li></ul>                                                                                                                                                                       |
| Remove desktop auto-update system           | Strip the entire desktop auto-update / update-manifest machinery from upstream.                                                                                                                                                      | `apps/desktop/src/updates/*` (deleted), `apps/desktop/src/electron/ElectronUpdater.ts` (deleted), `apps/desktop/src/ipc/methods/updates.ts` (deleted), `apps/web/src/lib/desktopUpdateReactQuery.ts` (deleted), `scripts/update-manifest.ts` / `merge-update-manifests.ts` / `mock-update-server.ts` (deleted), `.github/workflows/release.yml`                                                                                              | <ul><li>Removed update state machine, Electron updater, and IPC update methods</li><li>Removed web-side desktop-update React Query layer</li><li>Removed update-manifest build/merge/mock-server scripts</li><li>Trimmed release workflow accordingly</li></ul>                                                                                                                                                 |
| Project-level terminal support              | Allow terminals to be opened/scoped at the project level, not just per-thread.                                                                                                                                                       | `apps/server/src/terminal/Layers/Manager.ts`, `apps/server/src/terminal/Services/Manager.ts`, `packages/contracts/src/terminal.ts`, `apps/web/src/terminalStateStore.ts`, `apps/web/src/lib/terminalStateCleanup.ts`, `apps/web/src/components/ThreadTerminalDrawer.tsx`, `apps/web/src/components/CommandPalette.tsx`                                                                                                                       | <ul><li>Terminal manager/contracts extended to track project-scoped terminals</li><li>Terminal state store + cleanup reworked for project scope</li><li>UI (drawer, command palette, sidebar) wired to launch project terminals</li></ul>                                                                                                                                                                       |
| Project-scoped toasts                       | Make toasts carry environment + cwd context so they are scoped to the right project.                                                                                                                                                 | `apps/web/src/components/ui/toast.tsx`, `apps/web/src/components/ui/toast.logic.ts`, `apps/web/src/environments/runtime/service.ts`, `apps/web/src/components/GitActionsControl.tsx`                                                                                                                                                                                                                                                         | <ul><li>Toast payload extended with environment + cwd</li><li>Environment runtime service emits project-scoped toasts</li></ul>                                                                                                                                                                                                                                                                                 |
| Diff panel: file search + collapse-all      | Add file search and a collapse-all control to the diff panel.                                                                                                                                                                        | `apps/web/src/components/DiffPanel.tsx`                                                                                                                                                                                                                                                                                                                                                                                                      | <ul><li>Added file search filtering within the diff panel</li><li>Added collapse-all action for changed files</li></ul>                                                                                                                                                                                                                                                                                         |
| Full-screen plan modal                      | Add a full-screen plan modal to the chat view.                                                                                                                                                                                       | `apps/web/src/components/chat/FullScreenPlanModal.tsx`, `apps/web/src/components/ChatView.tsx`, `apps/web/src/components/PlanSidebar.tsx`, `apps/web/src/components/chat/ProposedPlanCard.tsx`                                                                                                                                                                                                                                               | <ul><li>New full-screen plan modal component</li><li>Plan sidebar / proposed-plan card wired to open it</li></ul>                                                                                                                                                                                                                                                                                               |
| Plan mode workflow                          | Manual plan promotion/revert/re-promotion, plan implementation in a new draft thread, and fallback capture for text-only plans.                                                                                                      | `apps/server/src/persistence/Layers/ProjectionThreadProposedPlans.ts`, `apps/server/src/persistence/Services/ProjectionThreadProposedPlans.ts`, `apps/server/src/orchestration/Layers/ProviderRuntimeIngestion.ts`, `packages/contracts/src/orchestration.ts`, `apps/web/src/components/chat/ComposerPrimaryActions.tsx`, `apps/web/src/components/chat/ComposerPlanFollowUpBanner.tsx`, `apps/server/src/textGeneration/*TextGeneration.ts` | <ul><li>Added manual plan promotion/revert commands + projection of revert state</li><li>Prevent plan promotion while user input is pending</li><li>Plan implementation can start in a new thread draft</li><li>Fallback capture for text-only plan-mode responses</li></ul>                                                                                                                                    |
| Session context tab + context meter         | Add a session context tab with metrics/breakdown and a context-window usage meter.                                                                                                                                                   | `apps/web/src/components/chat/SessionContextTab.tsx`, `apps/web/src/components/chat/ContextWindowMeter.tsx`, `apps/web/src/components/chat/sessionContextMetrics.ts`, `apps/web/src/components/chat/sessionContextBreakdown.ts`, `apps/web/src/components/chat/sessionContextFormat.ts`                                                                                                                                                      | <ul><li>New session context tab with metrics + breakdown</li><li>Context window meter with fallbacks and empty-value filtering</li><li>Tailwind group class scoped to the meter</li></ul>                                                                                                                                                                                                                       |
| Model picker provider filtering             | Hide unavailable providers in the model picker and add an OpenCode provider filter.                                                                                                                                                  | `apps/web/src/components/chat/ModelPickerContent.tsx`, `apps/web/src/components/chat/ProviderModelPicker.browser.tsx`, `packages/contracts/src/settings.ts`, `apps/web/src/components/settings/SettingsPanels.tsx`                                                                                                                                                                                                                           | <ul><li>New setting to hide unavailable providers</li><li>OpenCode provider filter in the model picker</li></ul>                                                                                                                                                                                                                                                                                                |
| Git / PR enhancements                       | Branch-centric terminology, head-repo support, customizable VCS prompts, and PR auto-create control.                                                                                                                                 | `packages/shared/src/git.ts`, `apps/server/src/sourceControl/*SourceControlProvider.ts`, `apps/server/src/textGeneration/TextGenerationPrompts.ts`, `apps/web/src/components/GitActionsControl.tsx`, `apps/web/src/components/BranchToolbarBranchSelector.tsx`, `apps/web/src/components/settings/SettingsPanels.tsx`                                                                                                                        | <ul><li>Replaced "ref" terminology with "branch" in UI + git actions</li><li>Preserve model-chosen branch name prefixes</li><li>Head-repository support added to PR/change requests</li><li>Customizable prompt instructions for VCS generation; sanitized PR bodies</li><li>Setting to disable auto-create PR on push</li><li>`isGitRepository` walks parent dirs so git subdirectories are detected</li></ul> |
| Settings reorg + font customization         | Reorganize settings into categorized sections and add terminal + diff font pickers.                                                                                                                                                  | `apps/web/src/components/settings/SettingsPanels.tsx`, `apps/web/src/components/settings/FontPicker.tsx`, `apps/web/src/lib/systemFonts.ts`, `packages/contracts/src/settings.ts`, `apps/web/src/components/ThreadTerminalDrawer.tsx`, `apps/web/src/components/DiffPanel.tsx`                                                                                                                                                               | <ul><li>Settings grouped into categorized sections</li><li>Font pickers for terminal and code diffs</li><li>System-font enumeration helper</li></ul>                                                                                                                                                                                                                                                            |
| Edit-message restore                        | Restore message text and attachments when reverting an edit.                                                                                                                                                                         | `apps/web/src/components/ChatView.tsx`, `apps/web/src/composerDraftStore.ts`                                                                                                                                                                                                                                                                                                                                                                 | <ul><li>Reverting an edit restores the original text + attachments to the composer</li></ul>                                                                                                                                                                                                                                                                                                                    |
| In-chat changed-files collapse/expand-all   | Collapse/expand-all control on the changed-files diff shown in the chat after a turn, with per-turn persistence and an expand-by-default setting.                                                                                    | `apps/web/src/components/chat/MessagesTimeline.tsx`, `apps/web/src/components/chat/ChangedFilesTree.tsx`, `apps/web/src/uiStateStore.ts`, `packages/contracts/src/settings.ts`, `apps/web/src/components/settings/SettingsPanels.tsx`                                                                                                                                                                                                        | <ul><li>Collapse all / expand all toggle on the in-chat changed-files section</li><li>Expand/collapse state persisted per thread + turn in the UI state store</li><li>`changedFilesExpandedByDefault` setting controls the initial state</li></ul>                                                                                                                                                              |
| Handle new Claude Agent SDK system subtypes | Handle the `system` message subtypes added by the `@anthropic-ai/claude-agent-sdk` `0.3.x` bump so they stop falling through to the generic "Unhandled subtype" runtime warning, and surface a dedicated tool-denied work-log entry. | `apps/server/src/provider/Layers/ClaudeAdapter.ts`, `packages/contracts/src/providerRuntime.ts`, `apps/server/src/orchestration/Layers/ProviderRuntimeIngestion.ts`                                                                                                                                                                                                                                                                          | <ul><li>`thinking_tokens` no-op'd (was flooding the work log with warnings while models think)</li><li>New `tool.denied` provider-runtime event + ingestion mapping → distinct error-tone work-log entry (tool + reason) instead of a generic "Runtime warning"</li><li>`mirror_error` mapped to `runtime.error`</li></ul>                                                                                      |

## Task Completion Requirements

- All of `bun fmt`, `bun lint`, and `bun typecheck` must pass before considering tasks completed.
- NEVER run `bun test`. Always use `bun run test` (runs Vitest).

## Project Snapshot

T3 Code is a minimal web GUI for using coding agents like Codex and Claude.

This repository is a VERY EARLY WIP. Proposing sweeping changes that improve long-term maintainability is encouraged.

## Core Priorities

1. Performance first.
2. Reliability first.
3. Keep behavior predictable under load and during failures (session restarts, reconnects, partial streams).

If a tradeoff is required, choose correctness and robustness over short-term convenience.

## Maintainability

Long term maintainability is a core priority. If you add new functionality, first check if there is shared logic that can be extracted to a separate module. Duplicate logic across multiple files is a code smell and should be avoided. Don't be afraid to change existing code. Don't take shortcuts by just adding local logic to solve a problem.

## Package Roles

### Apps

- `apps/server`: Node.js WebSocket server. Wraps Codex app-server (JSON-RPC over stdio), serves the React web app, and manages provider sessions.
- `apps/web`: React/Vite UI. Owns session UX, conversation/event rendering, and client-side state. Connects to the server via WebSocket.
- `apps/desktop`: Electron desktop shell (`@t3tools/desktop`). Hosts the web app in a native window and bridges to the server over IPC; Effect-based modules under `src/app`, with `backend`, `electron`, `ipc`, `ssh`, and `window` subsystems.
- `apps/marketing`: Astro marketing site (`@t3tools/marketing`).

### Packages

- `packages/contracts`: Shared effect/Schema schemas and TypeScript contracts for provider events, WebSocket protocol, and model/session types. Keep this package schema-only — no runtime logic.
- `packages/shared`: Shared runtime utilities consumed by both server and web. Uses explicit subpath exports (e.g. `@t3tools/shared/git`) — no barrel index.
- `packages/client-runtime`: Client-side runtime utilities (`@t3tools/client-runtime`) — advertised-endpoint resolution, known-environment detection, and source-control discovery state shared by web/desktop.
- `packages/effect-acp`: Effect bindings for the Agent Client Protocol (`effect-acp`) — the JSON-RPC client/agent/protocol layer the ACP runtime in `apps/server/src/provider/acp/` builds on.
- `packages/effect-codex-app-server`: Effect bindings for the Codex app-server JSON-RPC transport (`effect-codex-app-server`) — client/protocol/schema used by the Codex provider.
- `packages/ssh`: SSH utilities (`@t3tools/ssh`) — auth, command execution, config, and tunneling via subpath exports.
- `packages/tailscale`: Tailscale integration helpers (`@t3tools/tailscale`).

## Provider Architecture

T3 Code supports multiple coding-agent providers behind a uniform abstraction: **Codex, Claude, Cursor, and OpenCode**. Each has a driver (`apps/server/src/provider/Drivers/`), a provider layer (`apps/server/src/provider/Layers/*Provider.ts`), and a text-generation adapter (`apps/server/src/textGeneration/*TextGeneration.ts`). Providers that speak the Agent Client Protocol run through the shared ACP runtime in `apps/server/src/provider/acp/`.

The runtime pipeline is provider-agnostic:

- Provider dispatch and session lifecycle are coordinated in `apps/server/src/provider/Services/ProviderService.ts`.
- Provider runtime activity is projected into orchestration domain events in `apps/server/src/orchestration/Layers/ProviderRuntimeIngestion.ts`.
- WebSocket server routes NativeApi methods in `apps/server/src/ws.ts`; the web app consumes events via WebSocket push on channel `orchestration.domainEvent`.

**Codex** is the one provider with an unusual transport: it spawns `codex app-server` (JSON-RPC over stdio) per session, handled in `apps/server/src/provider/Layers/CodexSessionRuntime.ts` (+ `CodexAdapter.ts`). Docs: https://developers.openai.com/codex/sdk/#app-server
